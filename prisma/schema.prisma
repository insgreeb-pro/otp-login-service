generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = "file:../data.db"
}

model User {
  email  String  @id
  name   String?
  nim    String?
  avatar String?
}
